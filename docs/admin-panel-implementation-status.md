# Карта реализации административной панели

- Статус документа: живой реестр фактической реализации
- Обновлено: 2026-07-31
- Базовый коммит `main`: `cb7cca60d11f22ec18aa1751ec607ab30f6b3787`
- Основное ТЗ:
  [«Административная панель Академии Абрикософф»](admin-panel.md)
- Текущий production-режим: `owner_preview`

Этот документ отвечает на вопрос «что уже действительно работает», а не
заменяет архитектурное ТЗ. Требования, инварианты и критерии приёмки остаются
в [основном ТЗ](admin-panel.md). Карта обновляется после каждого принятого
изменения Administration.

## 1. Как читать статусы

| Статус | Значение |
|---|---|
| ✅ Готово | Код принят в `main`, обязательные проверки пройдены, требуемая для этапа приёмка выполнена |
| 🟡 Частично | Есть схема, типы, локальный прототип или часть сценария, но результат этапа ещё не достигнут |
| ⬜ Не начато | Рабочего пользовательского сценария и необходимого прикладного кода нет |
| ⛔ Нужна зависимость | Реализация требует решения, внешнего сервиса или операционного подтверждения |
| ⏸ После MVP | Возможность сознательно отложена до завершения операционного MVP |

Наличие будущего permission, типа, колонки или текста в ТЗ не считается
реализованной функцией.

## 2. Краткий ответ

Предыдущее ТЗ реализовано **не полностью**.

Сейчас готовы:

- защитный фундамент Administration;
- первый `owner`;
- единый первый вход администратора через один Telegram OIDC;
- отдельный Telegram step-up для уже открытой либо утратившей свежесть
  сессии;
- ограниченный production-preview `/admin`;
- безопасные переходы между личным кабинетом и админкой;
- выход из общей пользовательской и административной сессии;
- read-only список и компактная карточка ученика v2;
- достоверный read-only дашборд Administration;
- просмотр фактического оплаченного доступа;
- отзыв всех активных сессий выбранного ученика с идемпотентностью и аудитом;
- блокировка и разблокировка ученика с немедленным отзывом сессий,
  идемпотентностью, аудитом и защитой последнего доступного владельца;
- адаптивное сворачиваемое меню Administration;
- ежедневное обезличивание технического контекста сессий старше 12 месяцев
  установленной production-задачей.

В PR #34 приняты ADR-0008 и единый первоначальный Telegram-вход:

- ADR-0008 разделяет первоначальный административный вход и step-up уже
  существующей сессии;
- browser-bound Telegram-намерение `admin_login` для безопасного маршрута
  `/admin` и его дочерних маршрутов;
- серверная проверка отсутствия действующей сессии при старте и повторно в
  callback: существующая или появившаяся в гонке сессия переводится только в
  связанный `/admin/verify`;
- одна новая серверная сессия после первого Telegram OIDC с одинаковыми
  `authenticated_at` и `admin_verified_at`, фактическим методом и полным
  диагностическим контекстом;
- обычный `login`, demo/legacy-сессии и 12-часовой step-up не ослаблены;
- role, mode и permission по-прежнему перечитываются сервером на каждом
  защищённом запросе;
- полный автоматический раунд, независимые ревью и пользовательская приёмка
  реального Telegram OIDC пройдены до слияния;
- автодеплой merge-коммита и production smoke-проверка завершились успешно;
  production сохранил режим `owner_preview`.

В PR #36 принят фундамент ручного доступа:

- совместимая expand-миграция `access_manual_grants`, отделённая от
  `billing_access_grants`, с точными UTC-периодами, ссылкой на создавшую
  административную команду и защищённой историей `granted → revoked`;
- read-only `getEffectiveAccess(userId, at)`, который читает действующие
  оплаченные и ручные основания, не придумывает ручному доступу тариф или
  платёж и возвращает решение на одном моменте времени;
- закрытые режимы `legacy`, `shadow`, `v2` и
  `legacy_paid_plus_manual`, безопасный код несовпадения для будущего
  shadow-наблюдения и запрет `legacy`/`shadow` после появления первого
  ручного гранта;
- безопасные значения по умолчанию: `EFFECTIVE_ACCESS_MODE=shadow` и
  `MANUAL_ACCESS_GRANTING_ENABLED=false`; выдача разрешается только в `v2`;
- unit- и PostgreSQL integration-тесты схемы, неизменяемости, граничных
  моментов, объединения источников, paid-регрессии и конфигурационных
  ограничений;
- полный автоматический набор, независимые ревью и техническая приёмка
  пройдены до слияния;
- автодеплой merge-коммита, CI `main` и production smoke-проверка завершились
  успешно; production сохранил `owner_preview`, `shadow` и запрет выдачи.

PR #36 намеренно не подключал resolver к `getAccessContext` или
`getCabinetContext`, не создавал команду ручной выдачи и не добавлял UI.

В текущем рабочем пакете локально реализовано отдельное наблюдаемое
подключение:

- `getAccessContext` и `getCabinetContext` вызывают единый Access-сервис на
  одном полученном от PostgreSQL моменте и в одном read-only
  `REPEATABLE READ`-снимке;
- в режиме `shadow` новое решение вычисляется и сравнивается, но ученику
  применяется прежнее решение оплаченного пути;
- право читать курсы отделено от состояния платной подписки: ручное основание
  не создаёт в кабинете вымышленный тариф, платёж или активную подписку;
- конкурентная фиксация платежа либо возврата между двумя чтениями не создаёт
  ложное shadow-несовпадение, потому что legacy- и v2-решения читают один
  снимок;
- соединение с read-only транзакцией, которую не удалось откатить,
  уничтожается и не возвращается в пул;
- несовпадение увеличивает только метрику с закрытым безопасным кодом без
  внутреннего UUID ученика и персональных данных;
- инфраструктурный сбой проверки истории грантов или нового чтения не лишает
  ученика прежнего оплаченного доступа, а подтверждённый конфигурационный
  запрет `shadow` после ручного гранта не маскируется;
- режимы `v2` и `legacy_paid_plus_manual` сохраняют fail-closed поведение при
  сбое применяемого нового чтения;
- кабинет различает действующую платную подписку, ручной доступ без подписки
  и завершённый доступ: ученику с действующим ручным основанием не предлагают
  «восстановить» уже открытые курсы и не показывают вымышленное продление;
- выдача ручных грантов остаётся выключенной;
- локально пройдены lint, typecheck, 287 unit-тестов, 68 PostgreSQL
  integration-тестов, production-сборка, 3 headless E2E-сценария и production
  dependency audit без найденных уязвимостей.

Этот локальный пакет ещё не принят в `main` и не наблюдался в production.

Ещё не готовы три обязательных продуктовых блока операционного MVP:

1. production-вход через email и безопасная связь Telegram с email в одной
   учётной записи;
2. оставшиеся команды поддержки и ручное управление доступом;
3. платёжная диагностика.

В production `owner_preview` рабочие разделы по-прежнему намеренно закрыты.
Код режима `operational` показывает дашборд, учеников, оплаченный доступ и
команды отзыва сессий, блокировки и разблокировки и принимается локально на
тестовых данных. Ручные доступы, детальная платёжная диагностика, просмотр
аудита и курсы ещё не реализованы.

## 3. Статус по этапам ТЗ

| Этап | Статус | Что фактически готово | Что осталось |
|---|---|---|---|
| 0. Утверждение | 🟡 Частично | ADR-0005 принят; роль первого выпуска — `owner`; первый владелец назначен; Telegram-вход принят; CMS отделена от операционного MVP | Определить личный административный email владельца; подтвердить MFA этого ящика; выбрать production-провайдера писем и адрес отправителя; утвердить сетевой путь к API провайдера |
| 1. Защитный фундамент | ✅ Готово | Миграция ролей, административных сессий, идемпотентных команд и append-only-аудита; модуль Administration; `requireAdminContext`; Telegram-подтверждение; bootstrap первого владельца; wrapper; `/admin`, `403`; тесты. ADR-0008 и единый первоначальный Telegram-вход приняты в PR #34 | Результат этапа достигнут; production остаётся в ограниченном `owner_preview` до выполнения этапа 2 |
| Допуск `owner_preview` | ✅ Готово | ADR-0007; production-гейт; разрешения только `admin.enter` и `admin.preview`; статическая страница; переходы и выход | Режим не заменяет этапы 2–4 и не даёт операционных полномочий |
| 2. Два штатных способа входа | 🟡 Частично | Локальный demo email magic-link; SHA-256 challenge; типы `email_magic_link` и `break_glass`; Telegram OIDC | Production-доставка писем; GET/POST-split и browser binding по ТЗ; привязка Telegram и email к одному UUID в обоих направлениях; lifecycle методов; outbox и worker; межканальные уведомления; реализованный break-glass; production smoke-тест двух каналов; переход в `operational` |
| 3. Поддержка учеников | 🟡 Частично | Read-only список, поиск, фильтры, курсорная пагинация и компактная карточка ученика v2 приняты в `main`; проверенные данные Telegram OIDC и расширенный диагностический контекст новых сессий без токенов; достоверный дашборд принят в PR #29; отзыв всех активных сессий принят в PR #32; блокировка и разблокировка приняты в PR #35; схема ручных грантов и объединённый read-only resolver приняты в PR #36; наблюдаемое подключение обоих ученических контекстов в `shadow` реализовано локально без изменения UI | Принять shadow-подключение и наблюдать его в production; затем отдельно включить применяемый `v2`, реализовать команды и UI выдачи/отзыва; обновлять `last_seen_at` в рабочем запросном пути |
| 4. Платёжная диагностика | ⬜ Не начато | Billing существует как отдельный модуль ученического приложения | Административные списки заказов и платежей, таймлайн, безопасный просмотр webhook, сверка ожидающего платежа |
| 5. CMS курсов | ⬜ Не начато | Целевая архитектура описана в ADR-0006 | Утвердить ADR-0006; версии курса, медиатека, Markdown-редактор, предпросмотр, публикация, перенос первого курса |
| 6. Расширения | ⏸ После MVP | Границы описаны в ТЗ | Возвраты, команда администраторов, дополнительные роли, импорт Lovatop, массовые операции, экспорт, аналитика и passkey |

Операционный MVP считается готовым только после завершения этапов 0–4, а не
после появления стартовой страницы `/admin`.

## 4. Матрица возможностей

### 4.1. Вход и учётная запись

| Возможность | Статус | Фактическое состояние |
|---|---|---|
| Вход пользователя через Telegram | ✅ Готово | Работает через Telegram OIDC |
| Вход администратора через Telegram | ✅ Готово | Первоначальный вход в `/admin` создаёт свежую административную сессию одним OIDC; для уже открытой либо утратившей свежесть сессии сохраняется отдельный step-up. Сценарий принят в PR #34 и проверен пользователем через реальный Telegram OIDC |
| Локальный вход через email | 🟡 Demo | Доступен только вне production; ссылка возвращается вызывающему клиенту, реальное письмо не отправляется |
| Production-вход через email | ⬜ Не готово | В production `emailAuthMode` принудительно равен `disabled` |
| Telegram и email принадлежат одному пользователю | ⬜ Не готово | Полного безопасного `link_method` в обоих направлениях нет |
| Межканальные уведомления о входе | ⬜ Не готово | Очередь доставки и адаптеры ещё не реализованы |
| Break-glass | 🟡 Только фундамент | Колонки, типы, правила и runbook есть; исполняемой команды и полного сценария нет |

### 4.2. Защитный контур Administration

| Возможность | Статус | Фактическое состояние |
|---|---|---|
| Роли и первый `owner` | ✅ Готово | Роли хранятся в БД; включена только роль `owner`; владелец назначается bootstrap-командой |
| Отдельная административная сессия | ✅ Готово | Проверяется метод, свежесть и связанная пользовательская сессия |
| Серверная проверка permissions | ✅ Готово | Маршруты используют `requireAdminContext`; клиентское меню не является границей доступа |
| Append-only аудит | ✅ Фундамент | Таблица, ограничения и запись событий защитного контура реализованы |
| Идемпотентные административные команды | ✅ Фундамент и первая команда | Журнал исполнений и отзыв сессий с повторами, конфликтом request hash, lease и fencing приняты в PR #32 |
| Production `owner_preview` | ✅ Готово | Доступны только `admin.enter` и `admin.preview` |
| Production `operational` | ⛔ Закрыто гейтом | На production-origin запрещён до выполнения условий этапа 2 |

### 4.3. Рабочие функции владельца

| Возможность | Статус |
|---|---|
| Достоверная статистика | ✅ Read-only дашборд принят в PR #29: точные определения, единый снимок, предметные permissions и ссылки только на существующие списки; локальная приёмка и production-проверка `owner_preview` пройдены |
| Поиск и карточка ученика | ✅ Компактная карточка v2 принята в PR #27; включая проверенные данные Telegram OIDC без токенов, permission-aware проекцию email и Billing-полей и явную версию Telegram metadata; локальная пользовательская и production-проверки пройдены |
| Просмотр и отзыв сессий ученика | ✅ Read-only просмотр принят в PR #27; отзыв всех активных сессий с подтверждением, причиной, идемпотентностью, конкурентной сериализацией и аудитом принят в PR #32 |
| Блокировка и разблокировка ученика | ✅ Принято в PR #35: permission, POST, UI, идемпотентность в области `(principal_key, action, idempotency_key)`, атомарные статус/сессии/аудит, защита последнего владельца и конкурентные тесты; функциональная и UX-приёмка, автодеплой и production smoke-проверка пройдены |
| Просмотр фактического доступа | 🟡 Оплаченные гранты приняты в PR #27 и проверены; объединённый paid/manual resolver принят в PR #36; в текущей ветке оба ученических контекста подключены к нему в наблюдаемом `shadow`, но пакет ещё не принят и ручных грантов в production нет |
| Выдача и отзыв ручного доступа | ⬜ Не готово |
| Список заказов и платежей | ⬜ Не готово |
| Платёжный таймлайн и сверка | ⬜ Не готово |
| Просмотр административного аудита | ⬜ Не готово |
| Редактирование и публикация курсов | ⬜ Не готово |

Таблица `billing_access_grants`, появившаяся до Administration, хранит
происхождение **оплаченного** доступа. Принятая в PR #36 схема
`access_manual_grants` хранит отдельные ручные основания, но сама по себе не
реализует выдачу или отзыв: прикладные команды, аудит операции и UI ещё
отсутствуют.

### 4.4. Эксплуатация и наблюдаемость

| Возможность | Статус | Что осталось |
|---|---|---|
| CI и автоматический production-релиз неизменяемого образа | ✅ Готово | Поддерживать текущий release-процесс |
| Локальная production-подобная приёмка по постоянному HTTPS-адресу | ✅ Готово | Именованный Cloudflare Tunnel, отдельные база, Telegram-бот и OIDC-клиент; `operational` для локальных тестовых данных и безопасный `owner_preview`; порядок запуска и обязательного завершения сессии зафиксированы в [инструкции](operations/local-public-development.md) |
| Сигнал сбоя записи административного аудита | ✅ Фундамент | Подключить штатное внешнее оповещение при развитии эксплуатации |
| Сигнал доступности владельца | ⬜ Не готово | Реализовать `admin_available_owner_count` и alert |
| Общий сигнал очередей исходящих сообщений | ⬜ Не готово | Появится вместе с email outbox и межканальными уведомлениями |
| Фоновые задачи Administration | 🟡 Частично | Worker и ежедневный systemd timer обезличивания технического контекста сессий приняты в PR #27, установлены и успешно запущены в production; worker email/уведомлений ещё отсутствует |
| Хранение будущего email API-ключа | ⛔ До подключения провайдера | До добавления ключа привести production env к требованиям владельца, группы и режима из критериев приёмки и проверить secret-marker |

## 5. Что уже принято и чем подтверждается

| Изменение | Результат |
|---|---|
| PR #20 | Первоначальное ТЗ административной панели |
| PR #21 | Уточнённые защитные инварианты |
| PR #22 | Архитектура email-входа и административного контура |
| PR #23 | Защитный фундамент Administration |
| PR #25 | Безопасный `owner_preview` в production |
| PR #26 | Живая карта фактической реализации Administration и обязательные правила её обновления |
| PR #24 | Процесс разработки без staging на предзапуске и постоянный HTTPS-адрес локальной приёмки |
| PR #28 | Проверяемые SHA, три независимых контура ревью, журнал раундов и передача слияния только владельцу |
| PR #27 / `33539852b434f8b5dd959a721498282ce8006364` | Read-only список и карточка ученика v2, оплаченный доступ, диагностический контекст сессий и ежедневное обезличивание приняты в `main` |
| PR #29 / `01baee172c793bf19f1893f6ba24e93e798fd7f2` | Достоверный read-only дашборд Administration принят в `main`; CI, локальная пользовательская приёмка, автодеплой и production-проверка `owner_preview` успешны |
| PR #31 / `269cf93d21eabb49a4286d46350bcdf6de90c247` | Правила независимого ревью Codex приняты в `main`; фактические возможности Administration не изменились |
| PR #32 / `25b7ff319e38cdfeebc2b189149343eb33cbb152` | Отзыв всех активных сессий ученика с permission-проверкой, идемпотентностью, конкурентной сериализацией и append-only-аудитом принят в `main`; пользовательская приёмка, автодеплой и production smoke-проверка успешны |
| PR #33 / `08bf8ae23bc4b37864f480e6455e1488f613fcfb` | Правила доказательного шлюза независимого ревью приняты в `main`; фактические возможности Administration не изменились |
| PR #34 / `882ed2963457968fe6c8c889b0a811c909d42dd2` | Единый первый Telegram-вход администратора по ADR-0008 принят в `main`; повторный step-up сохранён только для существующей либо утратившей свежесть сессии |
| PR #35 / `24eee59b22bd5fe1d767315f2c4372dd046f7b94` | Блокировка и разблокировка ученика, защита последнего владельца, корректная область идемпотентности и адаптивное сворачиваемое меню приняты в `main`; пользовательская приёмка, автодеплой и production smoke-проверка успешны |
| PR #36 / `cb7cca60d11f22ec18aa1751ec607ab30f6b3787` | Защищённая схема ручных грантов, объединённый read-only resolver и fail-closed предохранители rollout приняты в `main`; ученический путь и UI в этом PR не переключались |
| Приёмка 2026-07-29 | Владелец вошёл в `/admin`; переходы между админкой и кабинетом, аватар и выход проверены пользователем |
| Локальная приёмка 2026-07-29 | Владелец вошёл через отдельный Telegram OIDC-клиент на `academy-dev.abrikosoff.com`; повторный вход и возврат в кабинет проверены |
| Локальная приёмка 2026-07-30 | Владелец проверил компактную карточку ученика v2 на `academy-dev.abrikosoff.com`; пользовательская приёмка пройдена |
| Production-проверка 2026-07-30 | Автодеплой merge-коммита PR #27, health-check и публичные страницы успешны; retention wrapper и unit-файлы сверены с образом, реальный oneshot завершён успешно, timer включён и активен |
| Локальная приёмка 2026-07-30 | Владелец проверил достоверный read-only дашборд на `academy-dev.abrikosoff.com`; пользовательская приёмка пройдена |
| Production-проверка 2026-07-30 | Автодеплой merge-коммита PR #29 и health-check успешны; production остался в `owner_preview`, поэтому операционный дашборд по-прежнему закрыт fail-closed |
| Локальная приёмка 2026-07-31 | Владелец отозвал собственные активные сессии из карточки ученика; немедленный переход на `/login` подтверждён |
| Production-проверка 2026-07-31 | Автодеплой merge-коммита PR #32, health-check и неавторизованный `/admin` успешны; production остался в `owner_preview`, команда отзыва закрыта предметным permission |
| Локальная приёмка 2026-07-31 | Владелец прошёл первоначальный вход в `/admin` через один реальный Telegram OIDC без второго экрана подтверждения; пользовательская приёмка PR #34 успешна |
| Production-проверка 2026-07-31 | Автодеплой merge-коммита PR #34 и CI для `main` успешны; `/api/health` вернул точный merge SHA, публичная страница открылась, а неавторизованный `/admin` перенаправил на вход; production остался в `owner_preview` |
| Локальная приёмка 2026-07-31 | Владелец заблокировал и разблокировал тестового ученика, подтвердил отзыв сессий, подписи состояний, цвет сообщения и адаптивную компоновку PR #35 |
| Production-проверка 2026-07-31 | Автодеплой merge-коммита PR #35 и CI для `main` успешны; `/api/health` вернул точный merge SHA, Administration и database — `ok`, защищённые маршруты перенаправили анонимного пользователя на вход; production остался в `owner_preview` |
| Production-проверка 2026-07-31 | Автодеплой merge-коммита PR #36 и CI для `main` успешны; `/api/health` вернул точный merge SHA `cb7cca60d11f22ec18aa1751ec607ab30f6b3787`, публичные страницы открылись, а анонимный `/admin` перенаправил на вход; production сохранил `owner_preview`, `shadow` и запрет выдачи ручных грантов |

Кодовая база на указанном базовом SHA содержит следующие административные
точки интерфейса:

- достоверный read-only дашборд `/admin` в режиме `operational`;
- операционный read-only список `/admin/students`;
- компактную read-only карточку `/admin/students/[userId]`;
- POST-маршрут `/api/admin/students/[userId]/status` и диалог блокировки или
  разблокировки с явными состояниями учётной записи и доступа;
- адаптивное сворачиваемое боковое меню с иконками;
- страницу повторного Telegram-подтверждения `/admin/verify`;
- специальное представление ответа `403` из
  `src/app/admin/forbidden.tsx`; отдельного маршрута
  `/admin/forbidden` этот файл не создаёт.

PR #34 заменил только двойной первоначальный Telegram OIDC:
подписанное намерение `admin_login` создаёт одну новую сессию сразу с
обычной и административной свежестью. Страница `/admin/verify` и ротация
сохраняются для уже открытой обычной либо утратившей 12-часовую свежесть
сессии. Публичный старт и callback проверяют отсутствие действующей сессии,
поэтому прямой вызов endpoint-а и гонка между этапами не обходят связанный
step-up. Отдельных маршрутов общего списка доступов, платёжной диагностики,
аудита и курсов пока нет. Production `owner_preview` не выдаёт
`dashboard.read`, `users.read`, `access.read`, `billing.read`,
`sessions.revoke` и `users.status.write`, поэтому дашборд, рабочие маршруты и
команды там закрыты сервером.

Указанный базовый SHA дополнительно содержит схему `access_manual_grants`,
read-only resolver обоих источников и конфигурационные предохранители rollout.
Текущая ветка подключает resolver к `getAccessContext` и `getCabinetContext`
в наблюдаемом `shadow`: применяется прежнее решение, а новый результат и сбои
фиксируются без внутреннего UUID ученика и персональных данных. Сбой получает
только случайный технический `incidentId` безопасного серверного журнала. Это
подключение ещё не входит в базовый SHA и не наблюдалось в production.

## 6. Ближайшие зависимости

До production-реализации email необходимо зафиксировать:

1. личный административный email владельца;
2. подтверждение MFA на этом email;
3. поставщика транзакционных писем;
4. адрес отправителя на подтверждённом домене;
5. прямой HTTPS-путь к API провайдера либо отдельный email-egress;
6. место и права хранения provider API key;
7. правила доставки и мониторинга worker.

Эти решения не блокируют локальную реализацию read-only-разделов и внутренних
команд поддержки, но блокируют завершение этапа 2 и безопасное включение
production-режима `operational`.

## 7. Очередь реализации

Очередь ниже сохраняет границы основного ТЗ. Номера — это логические пакеты,
а не номера будущих GitHub PR.

1. **Email Identity в production.** Общий magic-link для учеников и
   администраторов, безопасный `link_method`, lifecycle методов, browser
   binding, GET/POST-split, delivery outbox, email adapter, worker и
   secret-marker.
2. **Межканальная защита и operational-гейт.** Уведомления через Telegram и
   email, Bot API-egress, общий сигнал очереди, исполняемый break-glass и
   production preflight.
3. **Read-only Administration.** Список и карточка ученика приняты в PR #27;
   достоверный дашборд принят в PR #29.
4. **Команды поддержки и ручной доступ.** Отзыв всех активных сессий принят в
   PR #32; блокировка и разблокировка приняты в PR #35; схема ручных грантов и
   новый resolver приняты в PR #36. В текущем пакете локально реализовано
   отдельное наблюдаемое подключение `shadow`. Далее: принять и наблюдать его
   в production, отдельно включить применённое чтение `v2` и только затем
   реализовать команды и UI выдачи и отзыва.
5. **Платёжная диагностика.** Read-only Billing, события и сверка.
6. **Команда администраторов.** Второй владелец, дополнительные роли и
   одноразовая авторизация точного критического действия — после
   операционного MVP.
7. **CMS.** После отдельного утверждения ADR-0006 и завершения
   операционного MVP.

## 8. Критерий полного завершения предыдущего ТЗ

Фраза «предыдущее ТЗ полностью реализовано» допустима, когда одновременно:

- закрыты все критерии приёмки этапов 0–4;
- один пользователь входит через Telegram и email в один внутренний UUID;
- `/admin` показывает реальные данные, а не демонстрационные значения;
- владелец выполняет основные операции поддержки без SSH и SQL;
- ручной доступ не маскируется под платёж;
- платёжные проблемы диагностируются из панели;
- все изменяющие операции имеют серверное permission-решение,
  идемпотентность и аудит;
- CI, локальная приёмка и production smoke-тесты пройдены;
- режим `operational` включён отдельным принятым изменением.

CMS из этапа 5 является следующим продуктовым этапом. Она не блокирует
признание операционного MVP завершённым, но блокирует утверждение, что вся
целевая административная система вместе с управлением курсами готова.

## 9. Правила обновления карты

После каждого изменения:

1. указать PR и принятый SHA;
2. обновить только подтверждённые возможности;
3. приложить результат локальных тестов и требуемой приёмки;
4. для production-функции указать результат smoke-теста;
5. не переводить этап в «готово» из-за одной миграции, permission или
   статической страницы;
6. при расхождении карты с кодом считать код фактом, исправить карту в
   ближайшем PR и отдельно проверить, не нарушено ли ТЗ.
